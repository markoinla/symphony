# Per-worker installs / build outputs
*/node_modules/
*/.wrangler/
*/dist/

# Dashboard build output for linear-agent
linear-agent/dashboard/node_modules/
linear-agent/dashboard/dist/

# Local secrets (e.g. .secrets/admin-token consumed by ops scripts)
.secrets/
